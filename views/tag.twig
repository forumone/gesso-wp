{% extends "base.twig" %}

{% block content %}
  <pre>
    {{ dump() }}
  </pre>

  <h1>Tag.twig search results</h1>

  {% for post in posts %}
    <p>{{post.title}}</p>
  {% endfor %}

  {% include 'templates/pager.twig' %}

{% endblock %}
