{#
  /**
  * @file
  * Component for a Pager.
  *
  * Variables:
  * - modifier_classes: [string] Classes to modify the default component styling.
  * - heading: [string] Accessible heading.
  * - items: [array] The pager items. Each item is an object containing:
  *   - url: [string] URL of the item (optional).
  */
#}

<nav class="pager {{ modifier_classes }}" role="navigation" aria-labelledby="pagination-heading">
  <h4 id="pagination-heading" class="visually-hidden">{{ heading|default('Pagination')|raw }}</h4>
  <ul class="pager__items js-pager__items">
  {% spaceless %}
    {% if previous_link %}
      <li class="pager__item pager__item--previous">
        <a class="pager__link pager__link--previous" href="{{ previous_link }}" title="{{ 'Go to previous page' }}" rel="prev">
          <span class="visually-hidden">{{ 'Previous page' }}</span>
          <span aria-hidden="true">Previous</span>
        </a>
      </li>
    {% endif %}

    {% for page in pagination.pages %}
      {% if page.link %}
        <li class="pager__item">
          {% block page %}
          {% endblock %}
        </li>
      {% else %}
        <li class="pager__item pager__item--current">
          <span class="{{ page.class }}">{{ page.title }}</span>
        </li>
      {% endif %}
    {% endfor %}

    {% if next_link %}
      <li class="pager__item pager__item--next">
        <a class="pager__link pager__link--next" href="{{ next_link }}" title="{{ 'Go to next page' }}" rel="next">
          <span class="visually-hidden">{{ 'Next page' }}</span>
          <span aria-hidden="true">Next</span>
        </a>
      </li>
    {% endif %}
  {% endspaceless %}
  </ul>
</nav>
